{
  "name": "api",
  "version": "1.0.0",
  "description": "uvid",
  "main": "dist/index.js",
  "scripts": {
    "start": "tsc && node dist/index.js",
    "build": "tsc",
    "heroku-postbuild": "npm run build && cd client && npm install && npm run build:style && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.6",
    "tslint": "^6.1.1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^2.3.0",
    "typescript": "^3.8.3"
  }
}
